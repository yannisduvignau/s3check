#!/usr/bin/env python3
"""
Setup script for s3check package (backwards compatibility).

For modern installations, use:
    pip install .

For development:
    pip install -e ".[dev]"
"""

from setuptools import setup

# The actual configuration is in pyproject.toml
# This file exists for backwards compatibility with older tools
setup()
